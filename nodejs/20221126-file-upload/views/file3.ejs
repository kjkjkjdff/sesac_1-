<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>
        function fileUpload(){
                var form = document.getElementById('dynamicFile'); 
                var formData = new FormData(form);
                
               
                axios({
                    method: 'post',
                    url: '/dynamicFile', 
                    data: formData,
                    headers: {
                         'Content-Type': 'multipart/form-data',
                    },
                }).then((a) => { return a.data; })
                .then((d) => {
                    document.querySelector("img").src = d.path;
                });
            }
          
    </script>

</head>

<body>

    <form id ="dynamicFile" method="post" name="upload-single" enctype="multipart/form-data" >
       <fieldset style="width:300px;">
        <legend><h3>개인정보</h3></legend>
        <h4>아이디: <input type="text" name="id" placeholder="ID" /></h4>
        <h4>비밀번호 :<input type="password" name="pw" placeholder="PW" /></h4>
        <h4>이름 : <input type="text" name="name" placeholder="name" /></h4>
        <h4>나이 : <input type="text" name="age" placeholder="age" /></h4>

       </fieldset>
      
       <input type="file" name="userfile"><br/>
    
    
  
       <button type="button" onclick="fileUpload()">회원가입</button>
       </br>   
     </form>
       <!-- <img src="" width="200"/> -->
     <br>
     <img>
</body>
</html>


